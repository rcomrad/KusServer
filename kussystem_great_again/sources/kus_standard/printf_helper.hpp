// #pragma once

// //--------------------------------------------------------------------------------

// #include "core/metaprogramming.hpp"

// #include "kus_string.hpp"

// //--------------------------------------------------------------------------------

// namespace str
// {

// class PrintfHelper
// {
// public:
//     static void process(bool aData, char** aPtr) noexcept;
//     static void process(int aData, char** aPtr) noexcept;
//     static void process(double aData, char** aPtr) noexcept;
//     static void process(const char* aData, char** aPtr) noexcept;
//     static void process(const char* aData, char** aPtr) noexcept;

// protected:
//     static constexpr auto wrapper(bool aData) noexcept;
//     static constexpr auto wrapper(int aData) noexcept;
//     static constexpr auto wrapper(double aData) noexcept;
//     static constexpr auto wrapper(const char* aData) noexcept;
//     static constexpr auto wrapper(const char* aData) noexcept;

//     static void print(const char* aData) noexcept;
// };

// } // namespace str

// //--------------------------------------------------------------------------------
